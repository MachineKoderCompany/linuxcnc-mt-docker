# LinuxCNC Machinetalk Docker Image

Docker image for building and running LinuxCNC and Machinetalk support combined.

## How to use

Build and start the Docker container:

```bash
# build it
./docker-dev.sh -b
# run it
./docker-dev.sh
```

